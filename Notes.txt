COMMANDS:
dotnet new sln -n InvoiceProcessingSystem
dotnet new classlib -o src/Invoice.Core -n Invoice.Core -f net10.0
dotnet new classlib -o src/Invoice.Core -n Invoice.Core -f net10.0
dotnet sln InvoiceProcessingSystem.sln add src/Invoice.Core/Invoice.Core.csproj
dotnet new classlib -o src/Invoice.Utils -n Invoice.Utils -f net8.0
dotnet sln InvoiceProcessingSystem.sln add src/Invoice.Utils/Invoice.Utils.csproj
dotnet restore
dotnet build
dotnet new globaljson --sdk-version 8.0.100
dotnet sln InvoiceProcessingSystem.sln list

----
I'll create a short TODO plan for creating the solution and .NET 8 library projects.

Created 4 todos

Progress: TODO plan created.

Steps and commands (run in repo root):

- Create an empty solution (use repo name or your choice):
  ```
  dotnet new sln -n InvoiceProcessingSystem
  ```

- Create a .NET 8 class library (put projects under `src/`):
  ```
  dotnet new classlib -o src/Invoice.Core -n Invoice.Core -f net8.0
  ```

- Add the project to the solution:
  ```
  dotnet sln InvoiceProcessingSystem.sln add src/Invoice.Core/Invoice.Core.csproj
  ```

- Repeat for additional libraries:
  ```
  dotnet new classlib -o src/Invoice.Utils -n Invoice.Utils -f net8.0
  dotnet sln InvoiceProcessingSystem.sln add src/Invoice.Utils/Invoice.Utils.csproj
  ```

- Restore and build:
  ```
  dotnet restore
  dotnet build
  ```

Useful extras:
- Pin SDK (optional):
  ```
  dotnet new globaljson --sdk-version 8.0.100
  ```
  Or run `dotnet --list-sdks` to pick the installed 8.x SDK version.
- List solution projects:
  ```
  dotnet sln InvoiceProcessingSystem.sln list
  ```
- Open in VS Code:
  ```
  code .
  ```

Want me to create the solution and one library now in the workspace?